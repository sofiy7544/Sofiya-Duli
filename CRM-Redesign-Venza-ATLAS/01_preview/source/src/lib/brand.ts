import { tr } from './i18n';
/** Название из текущей CRM (metadata/sidebar). Меняется в одном месте. */
export const BRAND = { name: 'On Top Property', get tagline() { return tr('CRM агентства'); } };
